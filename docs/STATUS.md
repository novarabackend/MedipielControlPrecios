# Estado del proyecto

Fecha: 2026-01-27

## Resumen
- Repo creado y publicado en GitHub: novarabackend/MedipielControlPrecios.
- Estructura inicial con carpetas `backend/` y `frontend/`.
- Frontend inicializado con plantilla CoreUI Angular Admin Template.
- Backend pendiente de inicializacion (.NET 8).

## Estructura
- `frontend/`: plantilla CoreUI (Angular) ya clonada.
- `backend/`: carpeta vacia para API y servicios.
- `README.md`: descripcion general.

## Pendientes inmediatos
1. Inicializar solucion .NET 8 en `backend/` (API + servicios base).
2. Definir esquema de base de datos inicial (catalogo, marcas, proveedores, categorias, precios, alertas).
3. Configurar autenticacion con Keycloak (OTP por correo).
4. Integrar endpoints para carga de catalogo y reportes.

## Notas
- La propuesta funcional y estimacion se encuentran en `wireframes/PROPUESTA.html` (fuera del repo).
